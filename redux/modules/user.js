// imports

// actions

// action creators

// API actions

// initial state

const initialState = {
  isLoggedIn: false
};

// reducer
function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

// reducer function

// exports

// default reducer export

export default reducer;
